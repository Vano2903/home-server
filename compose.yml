services:
  # grafana
  grafana:

  # prometheus
  prometheus:

  # gotify
  gotify:
    image: gotify/server
    ports:
      - 8080:80
    environment:
      GOTIFY_DEFAULTUSER_PASS: "admin"
    volumes:
      - "./gotify_data:/app/data"
